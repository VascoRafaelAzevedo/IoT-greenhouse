# Placeholder frontend image so CI passes. Replace later with real build (React/Vue/etc).
FROM nginx:1.27-alpine

# Simple placeholder HTML
RUN printf '<!doctype html><html><head><meta charset="utf-8"><title>GardenAway Frontend</title></head><body><h1>GardenAway Frontend Placeholder</h1><p>Replace this with real build output.</p></body></html>' > /usr/share/nginx/html/index.html

EXPOSE 80

# Default nginx CMD kept