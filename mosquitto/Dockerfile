# Custom Mosquitto image (optional)
# Currently just inherits upstream. You can later add an entrypoint script
# to auto-generate password file if it is missing.
FROM eclipse-mosquitto:latest

# Example (commented) for future:
# COPY docker-entrypoint.sh /docker-entrypoint-init.d/01-gen-pass.sh
# RUN chmod +x /docker-entrypoint-init.d/01-gen-pass.sh
